name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Lint check 
      run: echo "Lint passed"
    - name: Build project
      run: echo "BUild complete"
    -name: Check README
      run: grep "project" README.md 

    
